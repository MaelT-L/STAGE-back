###############################
# PropriÃ©tÃ©s profil "localhost"
###############################

# Spring
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# Base de donnees
spring.jpa.defer-datasource-initialization=true
# Ci dessous pour créer les tables dans la base de données
spring.jpa.hibernate.ddl-auto=none

spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
# Ci dessous pour insérer des données dans les tables
spring.sql.init.mode=never
spring.datasource.url=jdbc:postgresql://diplotaxis-dev.v212.abes.fr:5433/abes-hello
spring.datasource.username=user
spring.datasource.password=user
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Donnees de demonstration
spring.sql.init.platform=demo

# Configuration des logs
logging.config=classpath:log4j2-localhost.xml
logging.level.root=INFO
logging.level.fr.abes=DEBUG
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO

jwt.token.secret=eyJhbGciOiJIUzI1NiJ9


